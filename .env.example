# Frontend data adapter. Mock remains the safe default when this is omitted.
NEXT_PUBLIC_DATA_MODE=mock

# Leave empty until an API Gateway base URL exists in a later backend phase.
NEXT_PUBLIC_API_BASE_URL=

